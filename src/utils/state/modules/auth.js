const state = {
  username: "John Doe",
};

const getters = {
  username: (state) => state.username,
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
